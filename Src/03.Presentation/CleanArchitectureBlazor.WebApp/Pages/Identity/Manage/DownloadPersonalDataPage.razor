@page "/download-personal-data"
@using CleanArchitectureBlazor.WebApp.Extensions

<PageContainer Title="Download Personal Data Page">
    <AlertMessage Message="@StatusMessage" AlertType="success" />
    <AlertMessage Message="@StatusMessage" AlertType="danger" />
    <AlertMessage Message="@StatusMessage" AlertType="warning" />
    <AlertMessage Message="@StatusMessage" AlertType="primary" />
    <button class="btn btn-outline-primary" @onclick="DownloadPersonalData">Download Personal Data</button>
</PageContainer>



@code {
    private string StatusMessage { get; set; }
    private async Task DownloadPersonalData()
    {
        Console.WriteLine("Clicked");
        StatusMessage = "2FA has been disabled.";
        // // Call your API or backend to download personal data
        // var response = await Http.GetAsync("api/account/download-personal-data");

        // if (response.IsSuccessStatusCode)
        // {
        //     var content = await response.Content.ReadAsByteArrayAsync();
        //     var fileName = "PersonalData.json"; // Set the file name
        //                                         // FileUtil.SaveAsAsync(fileName, content); // Save the file
        // }
        // else
        // {
        //     // Handle error (e.g., display a message)
        // }
    }
}