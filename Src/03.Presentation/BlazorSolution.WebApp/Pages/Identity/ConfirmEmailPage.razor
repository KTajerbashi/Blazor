@page "/Confirm-Email"

<PageContainer Title="Confirm Email Page">
    <AlertMessage Message="@StatusMessage" />
</PageContainer>

@code {
    private string StatusMessage { get; set; } = string.Empty;

    protected override void OnInitialized()
    {
        // Fetch the status message from the query string or backend
        // var uri = Navigation.ToAbsoluteUri(Navigation.Uri);
        // if (QueryHelpers.ParseQuery(uri.Query).TryGetValue("message", out var message))
        // {
        //     StatusMessage = message ;
        // }
    }
}